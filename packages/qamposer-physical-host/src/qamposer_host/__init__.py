"""qamposer-physical-host — FastAPI kiosk host for Entangible (milestone M2).

Runs the vision pipeline in-process, serves the display app over a single
self-signed HTTPS origin, broadcasts circuits over WebSockets, and optionally
reverse-proxies qamposer-backend.

Placeholder package — modules implemented by another agent (milestone M2).
"""
